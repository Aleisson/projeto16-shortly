Neste projeto você deve implementar o back-end, modelar e criar o banco de dados (relacional) de um sistema encurtador de URLs chamado Shortly: Links que cabem no bolso!

Variáveis de ambiente:
PORT= Se refere a porta que onde fazemos o deploy.
DATABASE_URL= Se referea a porta onde fazemos a conexão com BD.
TOKEN_SECRET= é palavra chave para gerar os tokens usando JWT;